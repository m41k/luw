#!/bin/bash

tuser=user$RANDOM
tpass=P@5s$RANDOM

echo $tuser $tpass


useradd -m $tuser -p `openssl passwd -1 $tpass`

#useradd -m $tuser


echo usermod --lock $tuser | at now + 2 minutes

#sleep 30

#userdel -rf  $tuser
